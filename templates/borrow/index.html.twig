{% extends 'base.html.twig' %}

{% block title %}Borrow index{% endblock %}

{% block body %}
    <h1>Borrow index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Date de début</th>
                <th>Date de fin</th>
                <th>Date de retour</th>
                <th>Item</th>
            </tr>
        </thead>
        <tbody>
        {% for borrow in borrows %}
            <tr>
                <td>{{ borrow.dateStart ? borrow.dateStart|date('Y-m-d') : '' }}</td>
                <td>{{ borrow.dateEnd ? borrow.dateEnd|date('Y-m-d') : '' }}</td>
                <td>{{ borrow.returnDate ? borrow.returnDate|date('Y-m-d') : '' }}</td>
                <td>{{ borrow.itemsCollection.title }}</td>
                <td>
                    <a href="{{ path('app_borrow_show', {'id': borrow.id}) }}">show</a>
                    <a href="{{ path('app_borrow_edit', {'id': borrow.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_borrow_new') }}">Create new</a>
{% endblock %}
