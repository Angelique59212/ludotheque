{% extends '../base.html.twig' %}

{% block title %}ItemsCollection{% endblock %}

{% block body %}
    <h1>ItemsCollection</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ items_collection.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ items_collection.title }}</td>
            </tr>
            <tr>
                <th>Author</th>
                <td>{{ items_collection.author }}</td>
            </tr>
            <tr>
                <th>Editor</th>
                <td>{{ items_collection.editor }}</td>
            </tr>
            <tr>
                <th>Number_player</th>
                <td>{{ items_collection.numberPlayer }}</td>
            </tr>
            <tr>
                <th>Playing_time</th>
                <td>{{ items_collection.playingTime }}</td>
            </tr>
            <tr>
                <th>Language</th>
                <td>{{ items_collection.language }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_items_collection_index') }}">back to list</a>

    <a href="{{ path('app_items_collection_edit', {'id': items_collection.id}) }}">edit</a>

    {{ include('items_collection/_delete_form.html.twig') }}
{% endblock %}
